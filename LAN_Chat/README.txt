On the server machine, run the Server.py

On the client machine(s), change the "server-ip" for the ip of the server, then launch Chat.exe (Or just launch Client.py *and* Client_input.py

*Optional: Change the port for the clients to recieve messages and a port for the server to send messages.